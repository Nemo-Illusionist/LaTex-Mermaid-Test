# GitHub Actions Workflows

Проект использует два оптимизированных workflow для раздельного тестирования компонентов.

## 📦 test-package.yml

**Цель:** Быстрое тестирование пакета `mermaid.sty`

**Триггеры:**
- Push/PR с изменениями в `mermaid-package/**`
- Ручной запуск (workflow_dispatch)

**Что делает:**
- Устанавливает минимальные зависимости (XeLaTeX, mmdc)
- Запускает `mermaid-package/test-package.sh`
- Проверяет 24 теста пакета
- Загружает результаты тестов как артефакты

**Время выполнения:** ~2-3 минуты

**Использование:**
```bash
# Триггерится автоматически при изменениях в mermaid-package/
git add mermaid-package/
git commit -m "Update mermaid package"
git push

# Или запустить вручную через GitHub UI
```

## 📄 build-project.yml

**Цель:** Полная сборка и тестирование LaTeX проекта

**Триггеры:**
- Push/PR с изменениями в `*.tex`, `build.sh`, `test.sh`
- **Исключает** изменения только в `mermaid-package/**`
- Ручной запуск (workflow_dispatch)
- Создание тегов `v*` (для релизов)

**Что делает:**
- Устанавливает полный набор зависимостей (XeLaTeX, mmdc, шрифты)
- Заменяет Arial → Liberation Sans для Linux
- Запускает `test.sh` (21 тест + полная сборка)
- Создаёт `main.pdf`
- Загружает PDF и логи как артефакты
- Создаёт GitHub Release при тегах `v*`

**Время выполнения:** ~5-7 минут

**Использование:**
```bash
# Триггерится автоматически при изменениях в основных файлах
git add main.tex
git commit -m "Update document content"
git push

# Создание релиза
git tag v1.0.0
git push origin v1.0.0
```

## 🎯 Оптимизация

**Раздельные workflow позволяют:**
- ✅ Не запускать полную сборку при изменениях только в пакете
- ✅ Не запускать тесты пакета при изменениях только в документе
- ✅ Экономить время CI/CD и GitHub Actions минуты
- ✅ Параллельное выполнение при изменениях в обеих областях

## 📊 Матрица выполнения

| Изменения | test-package.yml | build-project.yml |
|-----------|------------------|-------------------|
| Только `mermaid-package/` | ✅ Запускается | ❌ Не запускается |
| Только `main.tex` | ❌ Не запускается | ✅ Запускается |
| `mermaid-package/` + `main.tex` | ✅ Запускается | ✅ Запускается |
| Ручной запуск | ✅ Доступен | ✅ Доступен |
| Теги `v*` | ❌ | ✅ + Release |

## 🔧 Ручной запуск

Оба workflow можно запустить вручную через GitHub UI:
1. Перейти в раздел "Actions"
2. Выбрать нужный workflow
3. Нажать "Run workflow"
4. Выбрать ветку и запустить

---

**Документация:**
- [STRUCTURE.md](../../STRUCTURE.md) - полная структура проекта
- [TESTING.md](../../TESTING.md) - руководство по тестированию
