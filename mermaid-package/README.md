# Mermaid LaTeX Package v1.0.0

Кастомный LaTeX пакет для интеграции [Mermaid](https://mermaid.js.org/) диаграмм в LaTeX документы.

## 📦 Структура пакета

```
mermaid-package/
├── mermaid.sty              # Основной файл пакета
├── README.md                # Этот файл
├── MERMAID_USAGE.md         # Полное руководство пользователя
├── ERROR_HANDLING.md        # Руководство по обработке ошибок
├── CHANGES.md               # История изменений
├── IMPROVEMENTS_SUMMARY.md  # Сводка улучшений v1.0.0
└── examples/
    └── example-custom-dirs.tex  # Пример с кастомными директориями
```

## 🚀 Быстрый старт

### Требования

- XeLaTeX или LuaLaTeX (с поддержкой `--shell-escape`)
- Node.js и npm
- [Mermaid CLI](https://github.com/mermaid-js/mermaid-cli):
  ```bash
  npm install -g @mermaid-js/mermaid-cli
  ```

### Установка

1. Скопируйте папку `mermaid-package/` в ваш проект
2. Добавьте в преамбулу документа:

```latex
\documentclass{article}

% Добавляем путь к пакету
\makeatletter
\def\input@path{{mermaid-package/}}
\makeatother

% Загружаем пакет
\usepackage{mermaid}

\begin{document}
% Ваш контент...
\end{document}
```

3. Компилируйте с флагом `--shell-escape`:
```bash
xelatex --shell-escape document.tex
```

## ⚠️ Важно: Безопасность

**Этот пакет требует `--shell-escape` для работы!**

⚠️ **ВНИМАНИЕ:** Флаг `--shell-escape` позволяет LaTeX выполнять произвольные команды на вашем компьютере.

**Правила безопасности:**
- ✅ Используйте ТОЛЬКО с доверенными `.tex` файлами
- ✅ НЕ компилируйте документы из неизвестных источников
- ✅ Проверяйте код перед компиляцией
- ✅ В CI/CD используйте изолированные контейнеры

**Подробнее:** См. [SECURITY.md](../SECURITY.md) в корне проекта

### Базовый пример

```latex
\begin{mermaidenv}[width=10cm]
graph TD
  A[Start] --> B{Decision}
  B -->|Yes| C[Action]
  B -->|No| D[Alternative]
\end{mermaidenv}
```

## 📖 Документация

- **[MERMAID_USAGE.md](MERMAID_USAGE.md)** - Полное руководство с примерами
- **[ERROR_HANDLING.md](ERROR_HANDLING.md)** - Диагностика и решение проблем
- **[CHANGES.md](CHANGES.md)** - История версий и изменений

## ✨ Основные возможности

### Конфигурируемые директории

```latex
\usepackage[
  outputdir=build,        % Директория для временных файлов
  mermaiddir=diagrams     % Поддиректория для Mermaid файлов
]{mermaid}
```

### Детальное логирование

```latex
\usepackage[verbose]{mermaid}  % Включает детальные логи ошибок
```

### Автоматическая проверка зависимостей

Пакет автоматически проверяет наличие `mmdc` при загрузке и выводит понятные сообщения об ошибках.

## 🔧 Опции пакета

| Опция | Значение по умолчанию | Описание |
|-------|----------------------|----------|
| `outputdir` | `build` | Директория для `-output-directory` |
| `mermaiddir` | `mermaid-images` | Поддиректория для `.mmd` и `.png` файлов |
| `verbose` | `false` | Детальное логирование ошибок mmdc |

## 📝 Примеры использования

### Дефолтные настройки

```latex
\usepackage{mermaid}
% Диаграммы будут в: build/mermaid-images/
```

### Кастомные директории

```latex
\usepackage[outputdir=out, mermaiddir=img]{mermaid}
% Диаграммы будут в: out/img/
```

### С детальным логированием

```latex
\usepackage[verbose]{mermaid}
% Создаются .log файлы для каждой диаграммы
```

Полный пример в [`examples/example-custom-dirs.tex`](examples/example-custom-dirs.tex)

## 🧪 Тестирование

Пакет включает автоматические тесты для проверки корректности:

```bash
cd mermaid-package
./test-package.sh
```

**Что тестируется (24 теста):**
- Структура пакета (все файлы на месте)
- Зависимости (xelatex, mmdc)
- Синтаксис LaTeX пакета (опции, команды)
- Компиляция минимального документа
- Генерация Mermaid диаграмм
- Примеры с кастомными директориями

**Результат:**
```
Всего тестов:   24
Пройдено:       24
Провалено:      0
Успешность:     100%
```

## 🐛 Решение проблем

### mmdc не найден

```
Package mermaid Warning: mmdc (Mermaid CLI) not found!
```

**Решение:**
```bash
npm install -g @mermaid-js/mermaid-cli
mmdc --version  # Проверка
```

### Ошибка генерации диаграммы

1. Включите verbose режим: `\usepackage[verbose]{mermaid}`
2. Проверьте лог-файл: `build/mermaid-images/mermaid-1.mmd.log`
3. Проверьте синтаксис на [Mermaid Live Editor](https://mermaid.live/)

Подробнее в [ERROR_HANDLING.md](ERROR_HANDLING.md)

## 🔄 Версионирование

**Текущая версия:** v1.0.0

См. [CHANGES.md](CHANGES.md) для полной истории изменений.

## 📄 Лицензия

MIT License

## 🤝 Благодарности

- [Mermaid.js](https://mermaid.js.org/) - за отличный инструмент диаграмм
- LaTeX community

## 📚 Дополнительная информация

- **Совместимость:** XeLaTeX, LuaLaTeX
- **Поддержка OS:** macOS, Linux, Windows
- **CI/CD:** Поддержка GitHub Actions
- **Формат вывода:** PNG (SVG планируется в v1.1.0)

---

Для вопросов и предложений создайте Issue в репозитории.
