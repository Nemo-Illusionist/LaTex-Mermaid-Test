# История изменений

## v1.0.0 (2025-01-05)

### ✨ Новые возможности

#### Улучшенная обработка ошибок
- **Проверка mmdc при загрузке**: Автоматическая проверка наличия mmdc
  ```latex
  Package mermaid Info: mmdc found and ready to use
  ```

- **Опция verbose**: Детальное логирование ошибок mmdc
  ```latex
  \usepackage[verbose]{mermaid}
  ```
  Создает `.log` файлы для каждой диаграммы с полным выводом ошибок

- **Детальные сообщения об ошибках**: Указание конкретного файла и пути к логу
  ```
  Mermaid Error: Failed to generate diagram
  File: mermaid-images/mermaid-1.mmd
  Check log: mermaid-images/mermaid-1.mmd.log
  ```

- **Graceful degradation**: При отсутствии mmdc показывается инструкция по установке

#### Конфигурируемые директории
- **Опция `outputdir`**: Позволяет задать имя директории для `-output-directory`
  ```latex
  \usepackage[outputdir=out]{mermaid}
  ```
  Дефолт: `build`

- **Опция `mermaiddir`**: Позволяет задать имя поддиректории для Mermaid файлов
  ```latex
  \usepackage[mermaiddir=diagrams]{mermaid}
  ```
  Дефолт: `mermaid-images`

- **Комбинирование опций**:
  ```latex
  \usepackage[outputdir=build, mermaiddir=img/mermaid]{mermaid}
  ```

#### Версионирование
- Добавлена версия в `\ProvidesPackage`
- Информационные сообщения при загрузке пакета
- Логирование используемых настроек

#### Улучшенная документация
- Подробная документация в заголовке пакета
- Примеры использования в комментариях
- Файл `MERMAID_USAGE.md` с полным руководством

### 🔧 Технические улучшения

- Использование `kvoptions` для обработки опций пакета
- `\providecommand` вместо `\def` для возможности переопределения
- Более понятные имена переменных и команд
- Улучшенные комментарии на русском языке

### 🐛 Исправления

- Исправлена работа с `-output-directory` (симлинки)
- Корректная обработка абсолютных и относительных путей
- Fallback для случая без output-directory

### 📝 Документация

Новые файлы:
- `MERMAID_USAGE.md` - полное руководство пользователя
- `example-custom-dirs.tex` - пример использования кастомных директорий
- `PROJECT_REVIEW.md` - анализ проекта
- `CHANGES.md` - история изменений (этот файл)

### 🚀 Совместимость

- ✅ Обратная совместимость с предыдущими версиями
- ✅ Работает с XeLaTeX и LuaLaTeX
- ✅ Поддержка macOS, Linux, Windows
- ✅ Совместимость с GitHub Actions CI/CD

---

## Предыдущие версии

### v0.x (до 2025-01-05)

Начальная версия без версионирования:
- Базовая интеграция Mermaid в LaTeX
- Поддержка `\mermaid{}` команды
- Поддержка `\begin{mermaidenv}` окружения
- Захардкоженные пути `build/` и `mermaid-images/`
- Работа с `--shell-escape`

---

## План развития

### v1.1.0 (планируется)

- [ ] Кэширование диаграмм по MD5 хешу
- [ ] Опция для вывода в SVG формат
- [ ] Опциональная проверка наличия `mmdc` при загрузке
- [ ] Более детальные сообщения об ошибках
- [ ] Поддержка пользовательских тем Mermaid

### v1.2.0 (планируется)

- [ ] Параметры конфигурации `mmdc` (фон, масштаб, CSS)
- [ ] Автоматическое переиспользование существующих PNG
- [ ] Опция `draft` для ускорения компиляции
- [ ] Поддержка параллельной генерации диаграмм

### v2.0.0 (возможно в будущем)

- [ ] Интеграция с TikZ для векторного вывода
- [ ] Прямая интеграция без внешних зависимостей
- [ ] Поддержка других форматов диаграмм (PlantUML, GraphViz)

---

## Вклад в проект

Если вы хотите внести вклад:
1. Создайте Issue для обсуждения новой функции
2. Fork репозитория
3. Создайте feature branch
4. Отправьте Pull Request

## Благодарности

- Mermaid.js team за отличный инструмент диаграмм
- LaTeX community за помощь и вдохновение
- Все контрибьюторы проекта
