# Структура проекта

```
LaTex-Mermaid-Test/
├── main.tex                    # Главный LaTeX документ
├── config.tex                  # Конфигурация методички (название, автор и т.д.)
├── cover.tex                   # Титульный лист
├── vvedenie.tex                # Введение
├── build.sh                    # Скрипт сборки с умной двойной компиляцией
├── test.sh                     # 🧪 Скрипт автоматического тестирования (22 теста)
├── puppeteer-config.json       # Конфигурация Puppeteer для mmdc
│
├── .github/
│   └── workflows/
│       ├── test-package.yml    # CI: тестирование пакета (при изменениях в mermaid-package/)
│       └── build-project.yml   # CI: сборка проекта (при изменениях в *.tex, build.sh)
│
├── mermaid-package/            # 📦 Кастомный LaTeX пакет для Mermaid
│   ├── mermaid.sty             # Основной файл пакета
│   ├── test-package.sh         # 🧪 Тесты пакета (24 теста)
│   ├── test-minimal.tex        # Минимальный тестовый документ
│   ├── README.md               # Документация пакета
│   ├── MERMAID_USAGE.md        # Полное руководство пользователя
│   ├── ERROR_HANDLING.md       # Руководство по обработке ошибок
│   ├── CHANGES.md              # История изменений
│   ├── IMPROVEMENTS_SUMMARY.md # Сводка улучшений v1.0.0
│   └── examples/
│       └── example-custom-dirs.tex  # Пример с кастомными директориями
│
├── PROJECT_REVIEW.md           # Анализ проекта и рекомендации
├── STRUCTURE.md                # Этот файл
├── TESTING.md                  # Руководство по тестированию
│
├── build/                      # 🔨 Директория сборки (игнорируется git)
│   ├── main.pdf                # Скомпилированный PDF
│   ├── main.log                # Лог компиляции LaTeX
│   ├── main.aux                # Вспомогательные файлы LaTeX
│   ├── main.toc                # Оглавление
│   └── mermaid-images/         # Сгенерированные Mermaid диаграммы
│       ├── mermaid-1.mmd       # Исходник диаграммы 1
│       ├── mermaid-1.mmd.png   # PNG диаграммы 1
│       ├── mermaid-2.mmd       # Исходник диаграммы 2
│       └── mermaid-2.mmd.png   # PNG диаграммы 2
│
├── mermaid-images -> build/mermaid-images/  # Симлинк (игнорируется git)
│
└── main.pdf                    # 📄 Итоговый PDF (копия из build/)
```

## Описание компонентов

### Основные файлы документа

- **main.tex** - главный файл, собирает всё воедино
- **config.tex** - параметры методички (название, дисциплина, авторы)
- **cover.tex** - шаблон титульного листа
- **vvedenie.tex** - введение в методичку

### Сборка

- **build.sh** - умный скрипт сборки с автоматической двойной компиляцией
  - Первый проход: генерирует диаграммы и вспомогательные файлы
  - Второй проход: обновляет оглавление и перекрёстные ссылки (если нужно)
  - Цветной вывод прогресса
  - Проверка ошибок

### Mermaid Package

Кастомный LaTeX пакет для интеграции Mermaid диаграмм:

**Основные файлы:**
- `mermaid.sty` - код пакета (v1.0.0)

**Документация:**
- `README.md` - быстрый старт и основные возможности
- `MERMAID_USAGE.md` - полное руководство (~380 строк)
- `ERROR_HANDLING.md` - диагностика проблем (~450 строк)
- `CHANGES.md` - история версий
- `IMPROVEMENTS_SUMMARY.md` - детальная сводка улучшений

**Примеры:**
- `examples/example-custom-dirs.tex` - пример с кастомными директориями

### CI/CD

GitHub Actions с раздельными workflow для оптимизации:

**test-package.yml** - Быстрое тестирование пакета
- Триггер: изменения в `mermaid-package/**`
- Зависимости: XeLaTeX, mmdc (минимальный набор)
- Запускает: `mermaid-package/test-package.sh` (24 теста)
- Время выполнения: ~2-3 минуты
- Артефакты: результаты тестов пакета

**build-project.yml** - Полная сборка проекта
- Триггер: изменения в `*.tex`, `build.sh`, `test.sh`
- Исключает: изменения только в `mermaid-package/**`
- Зависимости: XeLaTeX, mmdc, шрифты для русского языка
- Замена шрифтов для Linux (Arial → Liberation Sans)
- Запускает: `test.sh` (22 теста + полная сборка)
- Время выполнения: ~5-7 минут
- Артефакты: main.pdf, логи сборки
- Release: создание релиза при тегах `v*`

## Workflow использования

### Локальная сборка

```bash
./build.sh
```

Результат: `main.pdf` в корне проекта

### Тестирование

**Тесты всего проекта (22 теста):**
```bash
./test.sh
```

**Что проверяется:**
- Зависимости (xelatex, mmdc, node)
- Структура проекта
- Успешность сборки
- Валидация PDF (размер, страницы, метаданные)
- Оглавление
- Mermaid диаграммы (количество, размеры)

**Тесты пакета mermaid.sty (24 теста):**
```bash
cd mermaid-package && ./test-package.sh
```

**Что проверяется:**
- Структура пакета
- Синтаксис LaTeX
- Опции пакета (outputdir, mermaiddir, verbose)
- Компиляция минимального документа
- Генерация диаграмм
- Примеры с кастомными настройками

См. [TESTING.md](TESTING.md) для подробностей

### Структура директорий при сборке

1. **build/** - создаётся скриптом, содержит все временные файлы
2. **build/mermaid-images/** - диаграммы Mermaid (.mmd и .png)
3. **mermaid-images** - симлинк на build/mermaid-images/ (для совместимости с VerbatimOut)
4. **main.pdf** - копируется из build/ в корень для удобства

### Использование mermaid пакета в других проектах

1. Скопируйте папку `mermaid-package/` в ваш проект
2. Добавьте в преамбулу:
```latex
\makeatletter
\def\input@path{{mermaid-package/}}
\makeatother
\usepackage{mermaid}
```
3. Компилируйте с `--shell-escape`

## Версии и зависимости

**Версия пакета:** mermaid.sty v1.0.0

**Требования:**
- XeLaTeX или LuaLaTeX
- Node.js и npm
- `mmdc` (Mermaid CLI): `npm install -g @mermaid-js/mermaid-cli`
- Шрифты: Arial (macOS/Windows) или Liberation Sans (Linux)

**LaTeX пакеты:**
- fontspec
- polyglossia
- geometry
- graphicx
- hyperref
- fancyvrb
- kvoptions

## Git workflow

**Игнорируются:**
- `build/` - временные файлы сборки
- `mermaid-images` - симлинк
- `*.pdf` - сгенерированные PDF (кроме releases)
- `*.aux`, `*.log`, `*.toc` - LaTeX временные файлы

**Отслеживаются:**
- Все исходники (.tex, .sty)
- Документация (.md)
- Конфигурация (build.sh, workflow)

## Дополнительные файлы

- **PROJECT_REVIEW.md** - детальный анализ проекта с оценками и рекомендациями
- **puppeteer-config.json** - конфигурация для работы Chromium в CI окружении

---

**Последнее обновление:** 2025-01-06
**Версия документа:** 1.0.0
